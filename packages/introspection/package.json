{
  "name": "@trpc-studio/introspection",
  "version": "0.2.1",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tanlethanh/trpc-studio",
    "directory": "packages/introspection"
  },
  "files": [
    "index.js",
    "index.mjs",
    "index.d.ts",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean --tsconfig tsconfig.json",
    "dev": "tsup src/index.ts --format cjs,esm --watch --dts --tsconfig tsconfig.json",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "release": "rm -rf dist && pnpm build && bun run prepare.ts && cd dist && pnpm install && npm publish --access public && cd .."
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@trpc/server": "^11",
    "zod": "^4"
  },
  "devDependencies": {
    "@trpc/server": "^11.7.1",
    "@types/node": "^20",
    "tsup": "^8.0.2",
    "typescript": "^5",
    "zod": "^4.0.16"
  }
}
